# Progress log — 2026-06-21

A single working session. Goal: land deliverables across the whole pipeline.

## TL;DR — where #1 (trained model) stands (updated 2026-06-29)

Best **bull-window** result: **graded-reward GRPO** (continuous bet×signal) — CR +52.7 %,
Sharpe 0.93 (2024-26, 14-eq), matches the paper's Sharpe (1.51 vs 1.57). **But the
honest-lens audit (the reviewer's `eval_labels`/`compare_paper` + `make_report` →
`docs/2026-06-29-results-report.html`) shows this is NOT a prediction breakthrough:** every
working model's label-fidelity **IC ≈ 0.13–0.24** (a signal ceiling); the 0.93 is bull
long-bias + reward concentration (in flat 2025-H1 graded has *higher* IC 0.25 yet loses
−4.3 %). Real gap vs paper = **drawdown** (ours ~5–8 % vs 2.8 %) + regime robustness. Full
ladder + every negative lever in the memo (`docs/2026-06-21-three-way-comparison-memo.md`).

**Lever 3 diagnosed (2026-06-29, no training) — drawdown is prediction-bound:**
`oracle_check` (perfect labels) → OOS SR 8.46 / **MDD 2.73 %** (= paper's 2.8 %), so the
pipeline/sizing are sound and paper-level drawdown is reachable *with good predictions*.
`sizing_compare` (vary sizing on graded's fixed calls) → the deployed long-only 1/8 rule
already gives the **lowest drawdown (8.2 %)**; long-short raises return but drawdown to ~19 %.
⇒ **the drawdown gap is a prediction problem, not sizing/reward** — can't size around IC-0.2.
With IC input-bound at ~0.2, the **only genuine lever is more predictive inputs/data**;
reward/sizing/RL/teacher all sit under that ceiling. (So a drawdown-reward cycle would be
wasted — the honest lens saved it.)

**PUSH (2026-06-29) — multimodal is the regime-robust alpha (reverses "mm useless"):**
Tabular HistGBM baseline → achievable IC. **Price/technical ceiling ≈ 0.24** (GBM 0.237 vs
LLMs 0.13–0.24; LLM under-extracts, ⊕GBM ensemble 0.23 — 0.62-correlated, no orthogonal add).
**Regime split (train 2024 / test flat 2025-H1):** price-only IC **inverts to −0.05** (root
of the regime/drawdown weakness), but **multimodal-only IC +0.199 and trades +4.2 %, SR
+0.43, MDD 16.7 % — the ONLY model positive in 2025-H1** (LLMs: graded −1.01, v1 −0.30).
**Robustness-checked (don't over-claim):** the single-split 0.199 was optimistic — 5-fold
CV multimodal-only IC = **+0.120** vs permutation null +0.01±0.04 → **z=+2.5, REAL but WEAK**
(p≈0.006); per-feature ICs ~0 ⇒ nonlinear/interaction signal (why LLM/linear miss it). So:
multimodal = a **real, weak, orthogonal, regime-HEDGE** signal (positive in the flat regime
where price inverts), not a dramatic alpha. Lever: extract it properly (structured features,
**regime-aware**) for a modest IC lift + drawdown robustness. Caveats: weak (0.12), small-n
2024-2025H1, MDD 16.7 % → needs fresh/longer multimodal data. First *verified* positive lever.

**(b) combined model — multimodal adds incrementally:** 2024-2025H1 5-fold CV IC: price-only
0.138, mm-only 0.095, **price+mm 0.175** (+0.037 incremental, robust). Confirms a combined
price+multimodal predictor beats price-alone. (c) LLM-with-structured-features remains — but
the GBM already extracts the combined signal, so a **hybrid (LLM decision + multimodal/GBM
overlay)** likely beats a full LLM retrain on ROI.

**Open questions / next options:**
1. **Bear-slice robustness** — the +52.7 % is a bull-window result; 2025-H1 (flat) was
   −4.3 %. Test on a down/sideways slice before claiming a real edge. *(highest priority)*
2. **Signal/label IC audit** — researcher feedback: predicted-class vs oracle Pearson ≈ 0.2
   **both in- and out-of-sample**. Equal in/out ⇒ not overfitting ⇒ a *signal ceiling* or a
   label-alignment issue. Audit snapshot↔label alignment + measure continuous IC (Spearman)
   and its time-stability. May gate everything downstream.
3. **Stronger teacher (Opus 4.8 vs Qwen3-30B)** — frontier distillation for a better SFT
   base; weigh vs the finding that reasoning quality wasn't the bottleneck (reward was), and
   API cost.
4. Apply the graded reward to the multimodal base; write-up.

**Reviewer feedback (2026-06-29) + response:**
- *"pred-class vs oracle Pearson ≈ 0.2 in- AND out-of-sample"* — reproduced on our OOS
  (Pearson 0.195, IC 0.21). The **in≈out equality rules out overfitting → it's a signal
  ceiling** (the input predicts the label only ~0.2 even on training data). Caveats: (a)
  pred-vs-*engineered-label* ≠ real alpha — must measure IC vs **raw forward return**; (b)
  0.2 IC is *high* by quant norms (0.05 good), so suspiciously high vs a raw return → check
  for leak/artifact; (c) Sharpe 0.93 with IC 0.2 ⇒ P&L rides on a few high-conviction days
  (graded reward concentrates there) ⇒ **bear-slice + Sharpe-decomposition needed.**
  Next diagnostics (cheap, gate everything): raw-return IC (in/out), snapshot↔label
  alignment audit, rolling-IC stability, Sharpe concentration.
- *"Opus 4.8 as teacher instead of Qwen3-30B?"* — feasible (Anthropic API) but **won't break
  the 0.2 ceiling**: reverse-reasoning distillation teaches *rationale*, not new input
  signal; a stronger teacher writes better post-hoc justifications of the same label. Useful
  only as an SFT-base *quality* upgrade, not a gap-closer. Deprioritize vs the cheap IC audit.

## Landed

### A — prompt-only LLM comparison row ✅
- Served `Qwen3-4B-Instruct-2507` (advertised as `Qwen/Qwen3-4B`) on DGX Spark
  node 1 via `sparkq` (vLLM, BF16, `--enforce-eager`).
- Ran `run_comparison --llm` → 3-way table. **Provenance verified**: 12/12 cached
  replies reproduced by the live model (temp-0 greedy), so the numbers are
  attributable to this exact model.
- **OOS 2024-01→2026-04, 12 large-caps, weekly:**
  | strategy | CR | Sharpe | MaxDD |
  |---|---|---|---|
  | equal-weight | +126% | 1.07 | 27.8% |
  | momentum 12-1 | +50% | 0.66 | 19.6% |
  | prompt-only LLM | +43% | 0.71 | **14.8%** |
- Key finding: LLM returns are the **least correlated** to the baselines
  (0.63–0.69 vs 0.90 between the quant pair) → a differentiated signal, lowest
  drawdown, but lags on raw return in a bull market. 58% of decisions bullish;
  8.2% emitted no parseable tag (→ HOLD) — a parse-rate leak to fix.

### B — memo + report ✅
- [`2026-06-21-three-way-comparison-memo.md`](2026-06-21-three-way-comparison-memo.md)
  + self-contained `compare_lab/output/report.html` (table, correlation,
  equity curves, caveats).

### C — Trading-R1 SFT v0 ✅ (sub-project 2 kickoff)
- `compare_lab/labeling.py` — Algorithm S1 volatility 5-class labels, forward
  returns (`shift(-τ)`), distribution matches paper Table 2
  (Strong-Sell→Strong-Buy: 3/12/38/32/15%).
  Unit-tested.
- `compare_lab/sft/build_dataset.py` — 4,224 (snapshot → templated thesis)
  examples, **trained on pre-2024 data only** (leak-safe vs the 2024-26 eval).
- `compare_lab/sft/train.py` — TRL + PEFT LoRA. Trained 1 epoch on Qwen3-4B
  (node 2, NVIDIA container): train_loss 1.24→0.53, **eval token-acc ~80%**.
  Adapter in `data/sft_adapter_v0/`. **GB10 backbone-compatibility gate cleared.**

### D — complete the 14-ticker universe ✅
- Added SPY/QQQ (`qf-lib-harness/research/add_etfs.py`); 14-ticker comparison in
  `compare_lab/output_14/`. *(Superseded by the data-store `prices.parquet`,
  which already has 14 tickers + `raw_close`.)*
- **Robustness result** (14 tk): equal-weight +143% / SR 1.04, momentum +52% /
  SR 0.70, **LLM +36% / SR 0.55 / MDD 16.4%**. Adding the ETFs *lowers* the LLM
  Sharpe (0.71→0.55) but it keeps the lowest drawdown — the prompt-only signal
  is universe-sensitive (longs the ETFs too). Another argument for training.

### Multi-modal data store (delivered mid-session) ✅
- `qflib_data_store.zip` → `data/qflib_data_store/`: price, news, fundamentals,
  analyst + insider sentiment, macro — all PIT-timestamped.
- Documented in [`DATA_STORE.md`](DATA_STORE.md). **Three delivered-data defects
  found and fixed (unit-tested):**
  - macro `release_date == date` leak → `macro_pit.py` → `macro_pit.parquet`.
  - empty insider txn-type → `insider_pit.py` → `sentiment_insider_pit.parquet`
    (only 15 open-market Purchases out of 1,332 → BUY).
  - two revenue XBRL tags → `fundamentals_pit.py` → `fundamentals_pit.parquet`
    (collapsed to one canonical `Revenue` line, preferring total `Revenues`).

## Checkpoint
- Branched `checkpoint/2026-06-21-llm-sft-data`; 4 grouped commits (LLM tooling,
  SFT v0, PIT fixes, docs). Session handoff artifacts gitignored. Tests:
  **40 passing** (was 23).

## Follow-on (external-review triage, `docs/TODO-EXTERNAL-REVIEW.md`)
- ✅ P0 checkpoint commits (branch `checkpoint/2026-06-21-llm-sft-data`).
- ✅ P1.1 fundamentals revenue normalization (`fundamentals_pit.parquet`).
- ✅ P1.2 multi-modal PIT snapshot join (`multimodal_context.py`, opt-in in
  `snapshot.py`).
- ✅ P1.3 parse-rate guardrail — `parse_decision_status`, `LLMProvider.parse_stats`,
  warn past 20% no-tag, surfaced in `run_comparison`. (LLMProvider also gained an
  opt-in `multimodal=` arg for the P2.2 multi-modal run.)

- ✅ P2.1 evaluate SFT v0 — served the LoRA (vLLM `--enable-lora`), probed vs
  base: **SFT v0 collapsed to 100% HOLD** (60/60) → all-cash → CR 0% / MDD 0%.
  Clean negative result: templated SFT teaches boilerplate, decision token
  degenerates to the majority class. Fix = balanced data + completion-only loss +
  teacher distillation + GRPO. (Cache isolated via new `VLLM_CACHE_DIR`;
  `tests/probe_sft_vs_base.py`.)

## Next (open tasks)
- ✅ **SFT v1** (the v0 fix): completion-only loss + class balancing **fixed the
  all-HOLD collapse**. Probe (60): v0 was Hold 60/60; v1 is Hold 24 / StrongBuy 23
  / Sell 11 / Buy 2 — a real distribution. Train eval token-acc 80 % → 98.6 %,
  loss 0.53 → 0.089 (`data/sft_adapter_v1/`, served as `sft-v1`). **Backtest
  (14-ticker, `output_sftv1/`): CR +29 %, Sharpe 0.53, MDD 7.9 %** — the most
  defensive strategy (½ the prompt-only drawdown) at near-equal Sharpe, and
  **NO_TAG 0 %** (vs prompt-only 8.2 %). Fixed the collapse; didn't lift return
  yet → teacher distillation + GRPO next.
- P2.2 re-run prompt-only LLM with full multi-modal snapshots (now feasible via
  `LLMProvider(multimodal=...)`); compare vs the price-only prompt baseline.
- ❌ **SFT v2 (teacher distillation)** (`feat/sft-distill-grpo`): distillation
  (`sft/distill.py`, Qwen3-30B-A3B, `/no_think`) done (250 theses), v2 LoRA trained
  (`data/sft_adapter_v2/`, served `sft-v2`), **backtest done** (`output_sftv2/`):
  **regression vs v1** — CR +34 %, Sharpe 0.46, MDD 20.7 % (v1 7.9 %), NO_TAG 9.2 %
  (v1 0 %). The long §8 thesis style raised drawdown *and* fails to terminate on
  ~9 % of inputs (not a token-budget bug — residual no-tags avg ~14.8k chars at
  4096 max-tokens). Verdict in the memo. **Keep v1.**
- ⚠️ **GRPO RL on the v1 base — evaluated, mixed** (`compare_lab/grpo/`, TRL
  GRPOTrainer, node 2 GB10, HF gen — vLLM+Ray broken on GB10; merges v1 LoRA →
  fresh GRPO LoRA; 300 balanced pre-2024 prompts, 1 epoch / 69 steps, decision-
  driven since structure/evidence reward a flat 0 on the terse v1 base). Backtest
  (`output_grpo/`): **CR +37 %, Sharpe 0.58 — best of the trained models**, but
  MDD 21.6 % (v1 7.9 %) and **10 % NO_TAG** (v1 0 %): in 159/164 cases it echoes
  the template menu `[[[STRONG_BUY|BUY|HOLD|SELL|STRONG_SELL]]]` instead of one
  class (not truncation). Weak/noisy training signal (decision reward flat, entropy
  ~0.026). **Verdict: v1 stays the keeper; GRPO promising-but-unfinished** — fix
  the echo (parse guardrail) + more epochs / higher sampling entropy. See the memo.
- 🟢 **Gap-closing cycle 1 — multimodal SFT→GRPO** (spec/plan `docs/superpowers/
  {specs,plans}/2026-06-25-multimodal-sft-grpo*`): the dominant gap vs the paper is
  the modality gap. Leak-safe **train 2024 / eval 2025-H1**, 12-equity, multimodal
  snapshots (news/fundamentals/sentiment/macro) fed to SFT + GRPO. Carried the GRPO
  fixes: harsher invalid-decision penalty (−2.5, kills the echo), higher temperature.
  Config/builders/run_comparison flags (68 tests), SFT-mm **v3** (token-acc 97.9 %),
  GRPO-mm (1 epoch, temp 1.0). **Eval (2025-H1) → did not close the gap:** OFF/ON
  ablation — multimodal helped *parse* (NO_TAG 8.3→1.3 %) not *returns* (both ~−6 %);
  trained models collapsed (SFT-mm near-all-SELL → all-cash CR≈0; GRPO over-long
  −10 %) in a flat window (equal-weight +5.3 %). Caveats: thin ~900-tok snapshot,
  short noisy 6-mo window, tiny train sets. Verdict in the memo.
  - **Lever 1 tested — richer context not the bottleneck:** enriched the snapshot
    (3-bucket news ≤50 + 12 sentiment, ~900→~2k tok), re-ran the prompt-only ablation
    → parse monotonically up (NO_TAG 8.3→1.3→0.6 %) but **CR worse** (−6.3→−8.4 %):
    more context = more confident/aggressive bets, not better calls. Didn't retrain
    (ablation-first de-risk).
  - **Lever 2 tested — regime isn't the excuse:** price-only sft-v1/GRPO on the same
    flat 2025-H1 window → **price-only SFT-v1 survives (+0.6 %, MDD 9.3 %)** (window is
    survivable), while multimodal versions are worse than their price-only counterparts
    (SFT-mm all-SELL; mm-GRPO −10 % vs price-GRPO −5.6 %). Negatives = method + training
    data (v1 7-yr/4.2k vs mm 1-yr/290), not regime. Remaining: anti-collapse training.
    Hardware aside **resolved**: GB10 does *not* cap RL — TRL GRPO vLLM colocate
    rollout (`--use-vllm`, TP=1, no Ray) verified working, **~77 s/step vs 232 s HF
    (~3×) + higher entropy** (0.026→0.10). Blocker was only KV-cache sizing (cap
    `vllm_max_model_length`). Deeper RL now feasible on GB10 — a tool for the
    anti-collapse cycle. (Training correctness was never the issue: SFT-mm 97.9 %.)
  - ❌ **Lever 3a — deeper GRPO on v1** (vLLM, num_gen 12, 2 epoch, temp 1.2, −2.5
    guardrail): made it **worse** (2024-26 Sharpe 0.33 vs prior GRPO 0.58 / v1 0.53;
    2025-H1 −2.6 %). RL depth is *not* the lever — entropy stuck ~0.028 (no exploration
    on the confident v1 base) so extra steps over-optimise the noisy decision reward
    (50 % StrongBuy drift); the −2.5 guardrail failed because the 10 % NO_TAG is the model
    **copying the prompt's literal menu** `[[[A|B|C|D|E]]]` (160/167) — a prompt bug, not
    RL. Real levers: exploration (entropy / less-confident base) + prompt design.
    Hardware/data-quantity ruled out.
  - ✅ **Prompt fix (echo)** — reworded `_PROMPT_HEADER` to drop the copyable
    `[[[A|B|C|D|E]]]` menu (example is now one valid class, `[[[BUY]]]`). Validated on
    base prompt-only 2025-H1: **NO_TAG 8.3 %→2.6 %, menu echoes 100 %→0 %** — the cheap
    prompt fix did in one line what the −2.5 RL guardrail couldn't in 4 h. Now the
    shared base for the two parallel tracks (3b regularization, exploration reward).
  - 🟢 **Two parallel tracks — anti-overfit SFT + exploration GRPO** (spec
    `docs/superpowers/specs/2026-06-29-anti-overfit-two-tracks-design.md`). Root cause
    of the collapses: small models over-fit to degenerate confidence (v1 entropy 0.028
    → no GRPO exploration; mm-SFT → all-SELL). Shared anti-overfit SFT recipe, then RL.
    New knobs: `--label-smoothing`/`--lora-dropout` (SFT), `--beta`/`--diversity-weight`
    + `diversity_reward`, `--vllm-max-len` (GRPO). Both ran SFT→GRPO in parallel on the
    two nodes. ✅ **Both diagnosed causes fixed (eval done):**
    - **Track A** (v1-reg GRPO, diversity 0.3 + β=0 + vLLM): StrongBuy collapse gone
      (24-26 mix BUY 47/HOLD 23/SELL 21), Sharpe **0.33→0.54**, NO_TAG 10→4.5 %.
      Recovered to v1/prior-GRPO level (not beyond).
    - **Track B** (mm-reg GRPO, regularized): **all-SELL collapse dissolved** (97 %→BUY
      49/HOLD 28/SELL 12); 2025-H1 CR +1.0 %, Sharpe −0.17 — best multimodal, edges v1
      (−0.30). NO_TAG 6 %.
    Verdict: the methods now work (collapse + exploration both repaired) but *repair*,
    not breakthrough — v1's defensive MDD 7.9 % is still unbeaten; surpassing it needs a
    stronger verifiable reward or more/real data, not more knobs. **Gotchas:** (1) TRL 1.6
    `label_smoothing_factor` × `assistant_only_loss` → `num_valid_tokens` AttributeError
    (dropped smoothing; regularize via dropout 0.15 + 1 epoch); (2) rich multimodal
    prompts reach ~4.2k tok → `vllm_max_model_length` must be ≥8192 (4096 crashed mm-GRPO).
  - 🟢 **Stronger verifiable reward — graded continuous (bet × signal)** (spec
    `docs/superpowers/specs/2026-06-29-graded-continuous-reward-design.md`). The repairs
    only reached v1's ceiling, so this attacks the *reward form*: replace the coarse 5×5
    decision matrix with a dense `graded_decision_reward = bet(±2/±1/0) × clip(make_signal,
    ±3)`, losing trades ×1.5 (asymmetric downside). `make_signal` factored out of
    `make_labels`; GRPO dataset carries a per-example continuous `signal`; `--graded`
    flag swaps it in. GRPO from the v1-reg SFT base + diversity 0.3 + β=0 + vLLM.
    The graded reward **climbed −1.58 → +0.33** over training (the healthiest GRPO curve
    yet) and gave the **best bull-window return** (2024-26, 14-eq): CR +52.7 %, Sharpe
    0.93, MDD 11.2 %, NO_TAG 3.7 % (diverse mix, no collapse). ⚠️ **Honest-lens audit
    corrects the "breakthrough" read** (`eval_labels`/`compare_paper`): graded's
    label-fidelity **IC = 0.19 — the same ~0.2 ceiling** as every model; in flat 2025-H1
    it has a *higher* IC (0.25) yet **loses −4.3 %** ⇒ the 0.93 is **bull long-bias +
    reward concentration, not prediction skill**. vs paper (2024-06–08): Sharpe 1.51 vs
    1.57 (comparable), **MDD 5.5 % vs 2.8 % (2× worse)**. So reward density made GRPO
    learnable + topped the bull window, but **did not break the prediction ceiling.** Real
    levers: **drawdown control + regime robustness** (the paper's real edge) and more
    predictive *inputs* (IC is input-bound). 11 reward unit-tests green.
- **Submodule cleanliness:** `qf-lib-harness` shows dirty from the parent (task-D
  `research/add_etfs.py` + local data). Decide: commit `add_etfs.py` inside the
  submodule, drop it (the data-store `prices.parquet` already has 14 tickers), or
  document the dirtiness as intentional (frozen submodule).
