# Progress log — 2026-06-21

A single working session. Goal: land deliverables across the whole pipeline.

## Landed

### A — prompt-only LLM comparison row ✅
- Served `Qwen3-4B-Instruct-2507` (advertised as `Qwen/Qwen3-4B`) on DGX Spark
  node 1 via `sparkq` (vLLM, BF16, `--enforce-eager`).
- Ran `run_comparison --llm` → 3-way table. **Provenance verified**: 12/12 cached
  replies reproduced by the live model (temp-0 greedy), so the numbers are
  attributable to this exact model.
- **OOS 2024-01→2026-04, 12 large-caps, weekly:**
  | strategy | CR | Sharpe | MaxDD |
  |---|---|---|---|
  | equal-weight | +126% | 1.07 | 27.8% |
  | momentum 12-1 | +50% | 0.66 | 19.6% |
  | prompt-only LLM | +43% | 0.71 | **14.8%** |
- Key finding: LLM returns are the **least correlated** to the baselines
  (0.63–0.69 vs 0.90 between the quant pair) → a differentiated signal, lowest
  drawdown, but lags on raw return in a bull market. 58% of decisions bullish;
  8.2% emitted no parseable tag (→ HOLD) — a parse-rate leak to fix.

### B — memo + report ✅
- [`2026-06-21-three-way-comparison-memo.md`](2026-06-21-three-way-comparison-memo.md)
  + self-contained `compare_lab/output/report.html` (table, correlation,
  equity curves, caveats).

### C — Trading-R1 SFT v0 ✅ (sub-project 2 kickoff)
- `compare_lab/labeling.py` — Algorithm S1 volatility 5-class labels, forward
  returns (`shift(-τ)`), distribution matches paper Table 2
  (Strong-Sell→Strong-Buy: 3/12/38/32/15%).
  Unit-tested.
- `compare_lab/sft/build_dataset.py` — 4,224 (snapshot → templated thesis)
  examples, **trained on pre-2024 data only** (leak-safe vs the 2024-26 eval).
- `compare_lab/sft/train.py` — TRL + PEFT LoRA. Trained 1 epoch on Qwen3-4B
  (node 2, NVIDIA container): train_loss 1.24→0.53, **eval token-acc ~80%**.
  Adapter in `data/sft_adapter_v0/`. **GB10 backbone-compatibility gate cleared.**

### D — complete the 14-ticker universe ✅
- Added SPY/QQQ (`qf-lib-harness/research/add_etfs.py`); 14-ticker comparison in
  `compare_lab/output_14/`. *(Superseded by the data-store `prices.parquet`,
  which already has 14 tickers + `raw_close`.)*
- **Robustness result** (14 tk): equal-weight +143% / SR 1.04, momentum +52% /
  SR 0.70, **LLM +36% / SR 0.55 / MDD 16.4%**. Adding the ETFs *lowers* the LLM
  Sharpe (0.71→0.55) but it keeps the lowest drawdown — the prompt-only signal
  is universe-sensitive (longs the ETFs too). Another argument for training.

### Multi-modal data store (delivered mid-session) ✅
- `qflib_data_store.zip` → `data/qflib_data_store/`: price, news, fundamentals,
  analyst + insider sentiment, macro — all PIT-timestamped.
- Documented in [`DATA_STORE.md`](DATA_STORE.md). **Three delivered-data defects
  found and fixed (unit-tested):**
  - macro `release_date == date` leak → `macro_pit.py` → `macro_pit.parquet`.
  - empty insider txn-type → `insider_pit.py` → `sentiment_insider_pit.parquet`
    (only 15 open-market Purchases out of 1,332 → BUY).
  - two revenue XBRL tags → `fundamentals_pit.py` → `fundamentals_pit.parquet`
    (collapsed to one canonical `Revenue` line, preferring total `Revenues`).

## Checkpoint
- Branched `checkpoint/2026-06-21-llm-sft-data`; 4 grouped commits (LLM tooling,
  SFT v0, PIT fixes, docs). Session handoff artifacts gitignored. Tests:
  **40 passing** (was 23).

## Follow-on (external-review triage, `docs/TODO-EXTERNAL-REVIEW.md`)
- ✅ P0 checkpoint commits (branch `checkpoint/2026-06-21-llm-sft-data`).
- ✅ P1.1 fundamentals revenue normalization (`fundamentals_pit.parquet`).
- ✅ P1.2 multi-modal PIT snapshot join (`multimodal_context.py`, opt-in in
  `snapshot.py`).
- ✅ P1.3 parse-rate guardrail — `parse_decision_status`, `LLMProvider.parse_stats`,
  warn past 20% no-tag, surfaced in `run_comparison`. (LLMProvider also gained an
  opt-in `multimodal=` arg for the P2.2 multi-modal run.)

## Next (open tasks)
- P2.1 evaluate the SFT v0 adapter: serve with vLLM+LoRA, run
  `run_comparison --llm`, compare vs the prompt-only row.
- P2.2 re-run prompt-only LLM with full multi-modal snapshots (now feasible).
- P3 teacher distillation (Qwen3-32B) → replace templated rationale → GRPO.
